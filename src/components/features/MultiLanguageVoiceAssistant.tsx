import { HumanVoiceAgent } from "@/components/features/HumanVoiceAgent";

// Rebuilt: now delegates to the human-like voice agent (Lovable AI + interruption + personalization).
export const MultiLanguageVoiceAssistant = () => {
  return <HumanVoiceAgent />;
};

export default MultiLanguageVoiceAssistant;
